# Heavy Compute MCP image (STUB — see BUILD_PLAN Phase 5).
# Planned tools: docker_run, sympy_solve, profile_script, etl_validate.
FROM python:3.12-slim
WORKDIR /app
# COPY server.py .
# RUN pip install --no-cache-dir mcp sympy
# ENTRYPOINT ["python", "server.py"]
