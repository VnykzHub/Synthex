"""
Tests for {{ module_name }}.{{ function_name }}

Auto-generated by artifact-factory
"""

import pytest

from {{ module_name }} import {{ function_name }}


# ---------------------------------------------------------------------------
# Happy-path tests
# ---------------------------------------------------------------------------


class Test{{ function_name|capitalize }}HappyPath:
    """Group: standard expected-use cases for {{ function_name }}."""

    def test_{{ function_name }}_returns_expected_result(self):
        """{{ function_name }} should return the correct value given valid input."""
        # Arrange
        input_data = {{ happy_path_input }}

        # Act
        result = {{ function_name }}(**input_data)

        # Assert
        assert result == {{ happy_path_expected }}
        assert isinstance(result, {{ happy_path_type }})

    def test_{{ function_name }}_handles_empty_input(self):
        """{{ function_name }} should handle minimal/empty input gracefully."""
        # Arrange
        input_data = {{ edge_case_input }}

        # Act
        result = {{ function_name }}(**input_data)

        # Assert
        assert result is not None


# ---------------------------------------------------------------------------
# Edge-case tests
# ---------------------------------------------------------------------------


class Test{{ function_name|capitalize }}EdgeCases:
    """Group: boundary and degenerate inputs for {{ function_name }}."""

    def test_{{ function_name }}_with_none_input(self):
        """{{ function_name }} should raise TypeError on None input."""
        with pytest.raises(TypeError):
            {{ function_name }}(None)  # type: ignore[arg-type]

    def test_{{ function_name }}_with_extreme_values(self):
        """{{ function_name }} should not overflow or silently truncate extreme values."""
        input_data = {{ extreme_input }}
        result = {{ function_name }}(**input_data)
        assert result is not None


# ---------------------------------------------------------------------------
# Error-handling tests
# ---------------------------------------------------------------------------


class Test{{ function_name|capitalize }}ErrorHandling:
    """Group: expected-exception and defensive-behavior tests for {{ function_name }}."""

    def test_{{ function_name }}_raises_on_invalid_type(self):
        """{{ function_name }} should raise TypeError for type-invalid arguments."""
        with pytest.raises(TypeError):
            {{ function_name }}({{ invalid_type_arg }})

    def test_{{ function_name }}_raises_on_out_of_range(self):
        """{{ function_name }} should raise ValueError for out-of-range arguments."""
        with pytest.raises((ValueError, IndexError, KeyError)):
            {{ function_name }}({{ out_of_range_arg }})

    def test_{{ function_name }}_graceful_on_missing_key(self):
        """{{ function_name }} should raise KeyError or return default for missing keys."""
        with pytest.raises((KeyError, TypeError)):
            {{ function_name }}({{ missing_key_arg }})
